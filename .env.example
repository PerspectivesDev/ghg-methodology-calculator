# GHG Methodology Calculator — environment variables
# Copy to .env and set values. Do not commit .env (secrets, local paths).

# Database file path (SQLite). Used by persistence layer.
DB_PATH=./data/ghg-calc.db

# Server port for API.
PORT=3000
