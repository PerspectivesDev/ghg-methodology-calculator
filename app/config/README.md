# Config

Environment and factor metadata. No secrets in repo — use `.env` (see `../.env.example`).

Factor and equation version metadata will live under `config/factors/` or DB migrations (later tasks).
