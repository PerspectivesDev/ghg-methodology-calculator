# Product spec: [Product name]

> Copy this template into your project. Fill it for **your** app. This repo contains no tech- or app-specific content.

## 1. Goals

- **Primary goal**: (One sentence.)
- **Secondary goals**: (Bullet list.)

## 2. Scope

### In scope

- (Feature or capability 1)
- (Feature or capability 2)

### Out of scope

- (Explicitly not in this project)

## 3. Tech stack (your choice)

- **Runtime / framework**: (e.g. .NET Core, Node, Python, etc.)
- **Data / persistence**: (e.g. Entity Framework, PostgreSQL, MongoDB, etc.)
- **Tests**: (e.g. xUnit, pytest, Jest — and coverage target)
- **Other**: (APIs, queues, frontend framework — as needed)

## 4. Modules (if applicable)

- **Module A**: (One-line responsibility.)
- **Module B**: (One-line responsibility.)

Use `docs/modules/` templates and `agents/prompts/MODULE-BOUNDARIES-PROMPTS.md` for clear boundaries when you have multiple modules.

## 5. API / contracts (if applicable)

- (List main endpoints or interfaces: method, path or name, request/response, errors.)
- Or: "See OpenAPI / Swagger / interface docs."

## 6. Project layout (define per app)

- Where does application code live? (e.g. `src/`, `app/`, `backend/`, `frontend/` — name and structure as your stack expects.)
- Where are tests? (e.g. `tests/`, `*Tests/`, next to code.)
- Config / env: (e.g. `appsettings.json`, `.env` — no secrets in repo.)

## 7. Success criteria

- (Measurable or observable criterion 1)
- (Criterion 2)

## 8. Open questions

- (Anything to decide or clarify.)

---

**Next**: Use this spec to fill `../sprints.yaml` and `../tasks.yaml` (by hand or with `agents/prompts/SPEC-TO-YAML-PROMPT.md`). Then implement task by task, test first.
